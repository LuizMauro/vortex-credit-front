{
  "buildCommand": "cd ../.. && npm run build --workspace=apps/shell",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/).*)", "destination": "/index.html" }
  ]
}
